{
  "name": "@tollgatehq/sdk",
  "version": "0.1.0",
  "description": "Discover data services by ENS name and pay for them per call over x402 on Hedera",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "README.md"],
  "keywords": ["x402", "ens", "hedera", "agent", "micropayments"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tollgate/agent": "workspace:*",
    "@tollgate/x402-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
