// Moved to @tollgate/discovery. This re-export keeps every `./directory.js` import in the agent unchanged.
export { EnsDirectory, type Directory, type EnsDirectoryConfig } from "@tollgate/discovery";
